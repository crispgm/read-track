<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }} - Read Track</title>
    <meta charset="UTF-8">
    <meta name="description" content="Personal reading list"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Fira+Sans&display=swap">
    <link rel="stylesheet" href="/public/static/css/style.css">
    <link rel="icon" type="image/x-icon" href="/public/static/image/logo.png">
  </head>
  <body>
    <main>
      <div class="wrapper">
        <nav class="menu">
          <span class="menu-item{% if path == "/index" %} menu-active{% endif %}">
            <a href="/">Home</a>
          </span>
          <span class="menu-item{% if path == "/page/dashboard" %} menu-active{% endif %}">
            <a href="/page/dashboard">Dashboard</a>
          </span>
        </nav>
        <h1>{{ pageTitle }}</h1>
        <div class="article">
            {{ content }}
        </div>
      </div>
    </main>
  </body>
</html>
